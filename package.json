{
  "name": "driver-expenses-bot",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "test": "npm run test:fixtures",
    "test:fixtures": "node tests/validate-fixtures.mjs",
    "render:uml": "java -jar tools/plantuml.jar -tpng docs/diagrams/*.puml"
  },
  "devDependencies": {
    "ajv": "^8.17.1"
  }
}
